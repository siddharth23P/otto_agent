.env
.venv
__pycache__
.DS_Store
langfuse
# per-run final-answer files (13.5) -- not something to commit
otto_output/
# agent/eval/memory_bench.py's LoCoMo dataset cache (download_locomo()) --
# a ~2.8MB download-on-demand copy, not hand-authored fixture data like
# agent/eval/golden/'s JSON files, so it doesn't belong in git history.
agent/eval/data/
